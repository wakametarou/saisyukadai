.user-new
  %h1.user-new__icon
    = link_to image_tag('logo.png', class: "user-new__icon__image"), "#"
  .user-new__form
    .user-new__form--title
      %h2 新規アカウントの作成
    .user-new__form--for
      -# = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      -#   = render "devise/shared/error_messages", resource: resource
      = form_for(@user, url: user_registration_path) do |f|
        = render "devise/shared/error_messages", resource: resource
        .nickname-field
          .nickname-field__text
            = f.label :ニックネーム, class: "bold"
            %span.require
              必須
          %br/
          = f.text_field :nickname, autofocus: true, autocomplete: "nickname"
        .email-field
          .email-field__text
            = f.label :メールアドレス, class: "bold"
            %span.require
              必須
          %br/
          = f.email_field :email, autofocus: true, autocomplete: "email"
        .password-field
          .password-field__text
            = f.label :パスワード, class: "bold"
            - if @minimum_password_length
              %em
                (7文字以上)
            %span.require
              必須
          %br/
          = f.password_field :password, autocomplete: "new-password"
        .confimation-field
          .confimation-field__text
            = f.label :パスワード確認, class: "bold"
          %br/
          = f.password_field :password_confirmation, autocomplete: "new-password"
        .user-verification
          %H3.clearfix
            本人確認
          %p
            安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
        .name-field
          .name-field__text.clearfix
            = f.label :お名前（全角）, class: "bold"
            %span.require
              必須
          %br/
          .name-field__flex.clearfix
            .name-field__flex--left
              = f.text_field :first_name
            .name-field__flex--right
              = f.text_field :last_name
        .name-kana-field
          .name-kana-field__text.clearfix
            = f.label :お名前カナ（全角）, class: "bold"
            %span.require
              必須
          %br/
          .name-kana-field__flex.clearfix
            .name-kana-field__flex--left
              = f.text_field :first_name_kana
            .name-kana-field__flex--right
              = f.text_field :last_name_kana
        -# .birthday-field
        -#   .birthday-field__text
        -#     = f.label :生年月日, class: "bold"
        -#     %span.require
        -#       必須
        -#   .birthday-select
        -#     %input.birthday{type: "date", value: "1980-01-01"}
        .birthday-field
          .birthday-field__text
            = f.label :生年月日, class: "bold"
            %span.require
              必須
            %br/
          .birthday-select
            = f.date_select :birthday, {use_month_numbers: true,start_year: 1900, end_year: (Time.now.year - 0), default: Date.new(1980, 1, 1)}, {class: "date_select"}
        %p.form-info
          ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。

              

        .actions
          = f.submit "次へ"
      %p#link-signin
        = link_to 'サインインの方はこちら', new_user_session_path
  .user-new__footer
  

